name: API Automation Tests

on:
  push:
    branches: [ master, main ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run tests
      run: |
        # 直接运行 pytest，服务器会由 conftest.py 自动管理
        pytest testcases/business/ -v
        pytest testcases/environments/ -v --ignore=testcases/environments/test_reqres.py

    - name: Run tests with Allure
      if: always()
      run: |
        pytest testcases/ -v --alluredir=./reports/allure_raw --ignore=testcases/environments/test_reqres.py || true

    - name: Upload Allure results
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: allure-results
        path: reports/allure_raw